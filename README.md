# CoreSDK Python

Auth, policy, observability. One import.

```python
from coresdk import SDK

sdk = SDK.from_env()
claims = sdk.authorize(token)
```

See [docs](https://coresdk.io/docs/getting-started/python) for full usage.
