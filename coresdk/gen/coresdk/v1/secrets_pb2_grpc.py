# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from coresdk.v1 import secrets_pb2 as coresdk_dot_v1_dot_secrets__pb2


class SecretsServiceStub(object):
    """SecretsService — chat-time secret resolution.

    Per ADR-0011 (chat-time-secrets-resolver), this is a thin RPC wrapper
    around `coresdk_jobs::secrets::resolve_for_job` with a `chat:<session_id>`
    job_id prefix. Same SecretCatalog, same walk semantics, same audit
    pipeline — different context surface than the K8s job path.

    Caller passes principals derived from the JWT (tenant, user, groups);
    the sidecar walks the catalog, checks grants, reads plaintext from
    the configured Vault provider, and returns name → plaintext bytes.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.ResolveSecret = channel.unary_unary(
                '/coresdk.v1.SecretsService/ResolveSecret',
                request_serializer=coresdk_dot_v1_dot_secrets__pb2.ResolveSecretRequest.SerializeToString,
                response_deserializer=coresdk_dot_v1_dot_secrets__pb2.ResolveSecretResponse.FromString,
                _registered_method=True)


class SecretsServiceServicer(object):
    """SecretsService — chat-time secret resolution.

    Per ADR-0011 (chat-time-secrets-resolver), this is a thin RPC wrapper
    around `coresdk_jobs::secrets::resolve_for_job` with a `chat:<session_id>`
    job_id prefix. Same SecretCatalog, same walk semantics, same audit
    pipeline — different context surface than the K8s job path.

    Caller passes principals derived from the JWT (tenant, user, groups);
    the sidecar walks the catalog, checks grants, reads plaintext from
    the configured Vault provider, and returns name → plaintext bytes.
    """

    def ResolveSecret(self, request, context):
        """Resolve one or more bundle names for a caller. Returns plaintext
        values keyed by the consumer-visible env-var name (SecretRef.name).

        Errors:
        PERMISSION_DENIED — caller's principals don't satisfy any grant
        on the requested bundle (or a `condition_rule`
        in the grant evaluated to deny).
        NOT_FOUND         — bundle name doesn't exist in the caller's tenant.
        UNAVAILABLE       — Vault provider not configured or unreachable.
        INTERNAL          — catalog snapshot or Vault read failed.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_SecretsServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'ResolveSecret': grpc.unary_unary_rpc_method_handler(
                    servicer.ResolveSecret,
                    request_deserializer=coresdk_dot_v1_dot_secrets__pb2.ResolveSecretRequest.FromString,
                    response_serializer=coresdk_dot_v1_dot_secrets__pb2.ResolveSecretResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'coresdk.v1.SecretsService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('coresdk.v1.SecretsService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class SecretsService(object):
    """SecretsService — chat-time secret resolution.

    Per ADR-0011 (chat-time-secrets-resolver), this is a thin RPC wrapper
    around `coresdk_jobs::secrets::resolve_for_job` with a `chat:<session_id>`
    job_id prefix. Same SecretCatalog, same walk semantics, same audit
    pipeline — different context surface than the K8s job path.

    Caller passes principals derived from the JWT (tenant, user, groups);
    the sidecar walks the catalog, checks grants, reads plaintext from
    the configured Vault provider, and returns name → plaintext bytes.
    """

    @staticmethod
    def ResolveSecret(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/coresdk.v1.SecretsService/ResolveSecret',
            coresdk_dot_v1_dot_secrets__pb2.ResolveSecretRequest.SerializeToString,
            coresdk_dot_v1_dot_secrets__pb2.ResolveSecretResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
